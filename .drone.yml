kind: pipeline
name: build


      - ./gradlew --no-daemon --info build

  - name: release
    image: node:14
    commands:
      - npx semantic-release
    environment:
      GITHUB_TOKEN:
        from_secret: github_token
    when:
      event:
        exclude:
        - tag

volumes:
  - name: deps
    temp: {}
